process_key: forecast_gold
target: gold.fct_rain_forecast_hourly
scope: production

sources:
  - ref: int_weather_forecast_hourly
    change_column: _updated_at

checkpoint:
  safety_lag: 15 minutes

runner:
  # Only publish marts in the forecast serving contract.
  select: "dim_grid bridge_ward_grid fct_rain_forecast_hourly fct_rain_forecast_current_hourly fct_rain_pressure_alert"
