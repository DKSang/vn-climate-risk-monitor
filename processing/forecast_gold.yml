process_key: forecast_gold
target: gold.fct_rain_forecast_hourly
scope: production

sources:
  - ref: int_weather_forecast_hourly
    change_column: _updated_at

checkpoint:
  safety_lag: 15 minutes

runner:
  # Chạy staging + intermediate + gold mart cho forecast
  select: "stg_open_meteo__weather_forecast_hourly int_weather_forecast_hourly fct_rain_forecast_hourly"
